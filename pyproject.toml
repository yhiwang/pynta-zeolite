[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pyntaz"
version = "0.1.0"
description = "Zeolite adsorbate placement and TS guess construction"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["pyntaz"]
